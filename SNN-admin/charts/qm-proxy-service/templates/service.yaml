apiVersion: v1
kind: Service
metadata:
  name: {{ include "qm-proxy-service.fullname" . }}
  labels:
    {{- include "qm-proxy-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "qm-proxy-service.selectorLabels" . | nindent 4 }}
