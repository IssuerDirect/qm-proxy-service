apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qm-proxy-service.fullname" . }}-test-connection"
  labels:
    {{- include "qm-proxy-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "qm-proxy-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
